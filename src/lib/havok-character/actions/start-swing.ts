/**
 * SwingMotion を生成し startSwing を呼び出す。
 * 判断 (pickAttackType) は ai/ 層、構築 (buildSwingMotion) は swing-builder。
 */
export { buildSwingMotion } from '../ai/swing-builder';
export type { BuildSwingOptions } from '../ai/swing-builder';
